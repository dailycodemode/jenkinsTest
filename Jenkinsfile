@Library("shared-lib-basic") _

pipeline {
    agent any

    stages {
        stage('Demo') {
            steps {
                echo 'Hello World'
                welcome("MultiBranch-Demo")
            }
        }
    }
}
