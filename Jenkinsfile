@Library("shared-lib-basic") _

pipeline {
    agent any

    stages {
        stage('Demo') {
            steps {
                echo 'Hello World perftest'
                welcome("MultiBranch-Demo")
            }
        }
    }
}
